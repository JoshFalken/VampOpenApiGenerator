# VampOpenApiGenerator

This project generates an YAML-swagger-file that can be used to generate an Client. For now it is still in the testing-phase. But it does generate a lot of specs, which can be seen 
